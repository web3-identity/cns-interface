import React from 'react';
import PageWrapper from '@components/Layout/PageWrapper';

const MyDomains: React.FC = () => {

  return <PageWrapper className="pt-72px">MyDomains</PageWrapper>;
};

export default MyDomains;
